<Window x:Class="PLCSiemensSymulatorHMI.Views.ShellConductorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PLCSiemensSymulatorHMI.Views"
        xmlns:converters="clr-namespace:PLCSiemensSymulatorHMI.Converters"
        mc:Ignorable="d"
        Title="PlcSiemensHMI" Height="450" Width="900" MinHeight="450" MinWidth="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <converters:BoolToBrushConverter x:Key="BoolToBrush" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<ContentControl x:Name="TopMenu" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>-->

        <ContentControl x:Name="PlcList" Grid.Row="1" Grid.Column="0"/>

        <ContentControl x:Name="ActiveItem" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>
